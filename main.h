#ifndef MAIN_H
#define MAIN_H
/* Standard C library */
#include <unistd.h>
#include <stdarg.h>
#include <limits.h>

/* Functions Header prototypes*/
int _printf(const char *format, ...);
int _putchar(char c);
int _putstr(char *s);

#endif
